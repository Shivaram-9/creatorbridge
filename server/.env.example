PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/creatorbridge
JWT_SECRET=change-this-to-a-long-random-string-in-production

# CORS/Socket.IO use origin reflection (credentials) so Capacitor WebView + Vite can reach this API alongside http://10.0.2.2:5000 from the emulator.
